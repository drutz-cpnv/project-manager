{% extends 'base.html.twig' %}

{% block title %}Mandat — {{ mandate.uid | strpad(3, '0') }}{% endblock %}

{% block headerBtn %}
    <div class="d-flex gap-2">
        {% if mandate.state is not same as(3) or mandate.state is not same as(4) %}
        {{include('admin/mandate/_accept_form.html.twig')}}
        {{include('admin/mandate/_refuse_form.html.twig')}}
        {% else %}
        {% endif %}
    </div>
{% endblock %}

{% block body %}
    {{ include('partials/mandate/_show.html.twig') }}
{% endblock %}