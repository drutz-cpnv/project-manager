<form method="post" class="form-button" action="{{ path('admin.mandate.copil.refuse', {'id': mandate.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('copilRefuse' ~ mandate.id) }}">
    <button type="submit" class="btn btn-outline-danger btn-sm">Refuser</button>
</form>
