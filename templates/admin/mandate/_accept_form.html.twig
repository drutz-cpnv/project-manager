<form method="post" class="form-button" action="{{ path('admin.mandate.copil.accept', {'id': mandate.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('copilAccept' ~ mandate.id) }}">
    <button type="submit" class="btn btn-outline-success btn-sm">Accepter</button>
</form>
