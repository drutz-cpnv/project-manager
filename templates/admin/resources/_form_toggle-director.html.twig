{% if student.user is not same as(null) %}

<form method="post" class="form-button" action="{{ path('copil.resource.director.toggle', {'id': student.user.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('toggleDireje' ~ student.user.id) }}">
    {% if student.isDirector %}
        <button type="submit" class="btn btn-outline-danger btn-sm">Destituer</button>
    {% else %}
        <button type="submit" class="btn btn-outline-warning btn-sm">Définir Directeur</button>
    {% endif %}
</form>

{% endif %}