{{ form_start(form) }}
    {{ form_row(form.user) }}
    {{ form_row(form.title) }}
    {{ form_row(form.isProjectManager) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}