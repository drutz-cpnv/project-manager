{% extends 'app_base.html.twig' %}

{% block body %}

    <div class="container">
        <div>
            <div class="header">
                <h1><a href="{{ path('app.home') }}" class="back-btn mr2">{{ feather('arrow-left') }}</a>Connexion</h1>
            </div>
            <div class="body">
                <form method="post">
                    {% if error %}
                        <div class="alert alert-danger" role="alert">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                    {% endif %}

                    {% if app.user %}
                        <div class="alert alert-success" role="alert">
                            Vous êtes actuellement connecter en tant que {{ app.user.fullname }}, <a href="{{ path('security.logout') }}">Se déconnecter</a>
                        </div>
                    {% endif %}

                    <div class="form-group mb4">
                        <label for="inputUsername" class="form-label">Username</label>
                        <input type="text" value="{{ last_username }}" name="email" id="inputUsername" class="form-control" autocomplete="email" required autofocus>
                    </div>
                    <div class="form-group">
                        <label for="inputPassword" class="form-label">Password</label>
                        <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
                    </div>
                    <input type="hidden" name="_csrf_token"
                           value="{{ csrf_token('authenticate') }}"
                    >

                    <button class="btn-primary btn-block mt2" type="submit">
                        Se connecter
                    </button>
                </form>
            </div>
        </div>
        <div class="gradient-bg">
            <div class="header">
                <p class="text-muted">Bienvenue sur</p>
                <h2>L’application de gestion des projets de la Junior Entreprise</h2>
            </div>
            <div class="body">
                <p class="mb4">Cette application est à destination des</p>
                <div class="stylized-list ml2">
                    <div class="list-item">
                        <div class="icon">{{ feather('tool') }}</div>
                        <p>Étudiants <span class="text-muted">en médiamatique du CPNV de Ste-Croix de 3e année, afin d’accéder à la gestion de leurs différents projets.</span></p>
                    </div>

                    <div class="list-item">
                        <div class="icon">{{ feather('activity') }}</div>
                        <p>Clients <span class="text-muted">afin d’accéder au suivi de leurs différents projets. Ainsi qu’à effectuer des demandes de mandat.</span></p>
                    </div>

                    <div class="list-item">
                        <div class="icon">{{ feather('shield') }}</div>
                        <p>Enseignants <span class="text-muted">afin de suivre et d'assister les étudiants dans leurs différentes tâches. Ainsi qu'à gérer la réception des nouvelles demandes de mandat.</span></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
